// Re-export the shared Supabase client + runtime config for storefront use.
export { getSupabaseClient } from '../../shared/supabaseClient.js';
export { getRuntimeConfig } from '../../shared/config.js';
